# 토스 Frontend Developer 면접 과제 🔥

## Getting started

```sh
yarn dev
```

## 구현 체크리스트

### 필수 기능

- [x] 적금 상품 데이터 조회 (React Query)
- [x] 목표 금액 입력
- [x] 월 납입액 입력
- [x] 저축 기간 선택 (6개월, 12개월, 24개월)
- [x] 월 납입액과 저축 기간에 맞는 적금 상품 필터링
- [x] 적금 상품 선택 기능
- [x] 예상 수익 금액 계산
- [x] 목표 금액과의 차이 표시
- [x] 추천 월 납입 금액 계산
- [x] 추천 상품 목록 (연이자율 상위 2개)
- [x] 탭 전환 기능 (적금 상품 / 계산 결과)

### 추가 기능

- [x] 입력 필드 유효성 검증 (숫자만 허용)
- [x] Error Boundary를 통한 에러 처리
- [x] 에러 상태 fallback UI 구현
- [x] 숫자 포맷팅 (천 단위 콤마)

## 프로젝트 구조

```
src/pages/SavingsCalculatorPage/
├── index.tsx                    # 메인 페이지 컴포넌트
├── error.tsx                    # 에러 fallback UI
├── utils.ts                     # 유틸리티 함수 (입력 파싱)
└── useSavingsProduct/
    ├── index.ts                 # 커스텀 훅 (상태 + 비즈니스 로직)
    ├── api.ts                   # React Query API 레이어
    └── types.ts                 # 타입 정의
```

---

## 설계 철학: 추상화와 유지보수성

### 1. 추상화와 유지보수성의 관계

#### 1.1 두 관점의 공통점

- 둘 다 **인지 부하를 낮추는 것**이 목표입니다
- 둘 다 **적절한 경계 설정**이 핵심입니다

#### 1.2 두 관점의 차이점

| 관점           | 접근 방법                   | 측정 기준          |
| -------------- | --------------------------- | ------------------ |
| **추상화**     | 정보의 압축 (복잡성 숨기기) | "이해하기 쉬운가?" |
| **유지보수성** | 변경 범위의 특정 (격리)     | "수정하기 쉬운가?" |

**추상화**는 적절한 정보의 압축을 통해 인지 부하를 낮춥니다. 보여줘야 할 것은 보여주고, 감춰야 할 것은 감춤으로써 코드를 이해하기 쉽게 만듭니다.

**유지보수성**은 변화 시점과 이유가 동일한 것끼리 뭉치고, 다른 것끼리 나눔으로써 변경 범위를 특정합니다. 이러한 변화율에 따른 격리가 성공했을 때 유지보수성이 높아집니다.

변화율을 결정하는 요인은 해당 제품의 **의사결정 구조**와 관련이 있다고 생각합니다. 의사결정이 누구에 의해, 어떤 단위로 일어나는지 정보를 축적하면서 변화율을 감지하고 그 단위로 격리에 성공하면 유지보수성이 높아진다고 생각합니다.

#### 1.3 두 관점 사이의 긴장 (개인적 관찰)

추상화와 유지보수성은 둘 다 인지 부하를 낮추는 것이 목표지만, **이 둘이 항상 같은 방향을 가리키는 것은 아니라고 생각합니다.**. 관점이 다르기 때문에 긴장이 있어 보입니다. 추상화는 정보의 압축을 통해 복잡성을 숨기려 하고, 유지보수성은 변경 범위를 명확히 특정하려 하는 것처럼 느껴지기 때문입니다.

##### 이 프로젝트의 선택과 한계

이 프로젝트에서는 **기능 응집(Functional Cohesion)만 달성**하는 선에서 멈췄습니다.

**왜 더 깊은 추상화가 보이지 않았는가?**

이 훅은 기능의 depth가 얕아서 "비즈니스 로직"이라는 기능 단위로만 묶는 것 이상의 추상화가 자연스럽게 드러나지 않았습니다.

예를 들어:

- `{ state, actions }` 형태로 압축하려 해도, 상태들 간의 명확한 그룹핑 기준이 보이지 않았습니다.
- 도메인 객체로 추상화하려 해도, "적금 계산"이라는 개념이 객체로 응집될 만큼 복잡하지 않았습니다.

기능이 더 복잡해지고 depth가 깊어지면, 추상화할 지점이 드러날 수도 있다고 생각합니다. 현재는 그런 지점을 발견하지 못했습니다.

**성공한 추상화 사례**

이 프로젝트에서 `parseNumericInput` 함수는 적절한 추상화를 찾은 경우입니다. "숫자 입력 검증"이라는 개념이 명확했고, 여러 곳에서 동일하게 재사용되므로 함수로 추출하는 것이 자연스러웠습니다.

---

### 2. 이 과제를 진행하면서의 의사결정

#### 2.1 비즈니스 로직과 UI의 격리

비즈니스 로직을 `useSavingsProduct` 훅으로 분리했습니다. 이를 통해 UI 변경과 독립적으로 로직을 수정할 수 있게 되었습니다.

```tsx
// ❌ 나쁜 예: UI와 로직이 결합됨
function SavingsCalculatorPage() {
  const [products, setProducts] = useState([]);
  const [monthlyAmount, setMonthlyAmount] = useState(null);

  // 필터링 로직이 UI 컴포넌트 안에 있음
  const filteredProducts = products.filter(
    product => monthlyAmount >= product.minMonthlyAmount && monthlyAmount <= product.maxMonthlyAmount
  );

  return <div>{/* UI 렌더링 */}</div>;
}

// ✅ 좋은 예: 비즈니스 로직을 훅으로 분리
function SavingsCalculatorPage() {
  const { filteredProducts, setMonthlyAmount } = useSavingsProduct();
  return <div>{/* UI 렌더링 */}</div>;
}
```

예를 들어, 디자이너가 레이아웃을 변경하더라도 계산 로직에는 영향이 없고, 반대로 프로덕트 오너가 이자 계산 공식을 변경하더라도 UI 컴포넌트는 전혀 수정할 필요가 없습니다.

#### 2.2 상태 의존성에 따른 응집

상태들을 서로 의존성이 있는 것끼리 묶어보니 현재 `useSavingsProduct` 훅에서 관리하는 상태 정도로 묶였습니다.

```tsx
// useSavingsProduct 내부
const filteredProducts = useMemo(() => {
  if (monthlyAmount === null) return [];
  return products.filter(
    product =>
      monthlyAmount >= product.minMonthlyAmount &&
      monthlyAmount <= product.maxMonthlyAmount &&
      product.availableTerms === savingsTerm
  );
}, [products, monthlyAmount, savingsTerm]);

const recommendedProducts = useMemo(() => {
  return [...filteredProducts].sort((a, b) => b.annualRate - a.annualRate).slice(0, 2);
}, [filteredProducts]);
```

예를 들어 `selectedTab`(적금 상품 / 계산 결과)은 `products` 데이터나 파생 상태들과 전혀 엮이지 않기 때문에 이 훅에 포함되지 않고 페이지 컴포넌트에서 독립적으로 관리됩니다.

#### 2.3 입력 검증 로직의 재사용

목표 금액과 월 납입액 입력 필드는 동일한 검증 규칙을 따릅니다. 숫자만 허용하고, 콤마를 제거하며, 빈 값을 처리하는 로직이 반복될 수 있습니다.

```tsx
// utils.ts
export const parseNumericInput = (input: string): number | null | undefined => {
  const value = input.replace(/,/g, '');
  if (value === '') return null;
  if (/^\d+$/.test(value)) return Number(value);
  return undefined;
};

// 사용처
onChange={e => {
  const parsed = parseNumericInput(e.target.value);
  if (parsed !== undefined) {
    setTargetAmount(parsed);
  }
}}
```

이를 `parseNumericInput` 함수로 추출하여 두 입력 필드에서 재사용했습니다. 이로써 검증 로직의 일관성을 확보했을 뿐만 아니라, 향후 검증 규칙이 변경되더라도 함수 하나만 수정하면 모든 입력 필드에 변경사항이 반영됩니다.
